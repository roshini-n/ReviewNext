import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.naguru.reviewnext',    // Unique ID for your app
  appName: 'ReviewNext',             // Name displayed on mobile
  webDir: 'dist/game-logd/browser',         // Angular production build folder
};

export default config;
